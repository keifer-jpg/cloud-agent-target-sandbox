# Cloud Agent — Plan Report

**Status:** work-ready

## Overall goal
RESUME-VALIDATION plan (throwaway; for Phase 3 resume-on-crash live testing). PURPOSE: give a PREDICTABLE window to cancel the Render job mid-Phase-2, AFTER Phase 1 has durably checkpointed, so we can prove the run resumes at Phase 2 without rebuilding Phase 1 or double-spending. HARD RULES: smallest possible PURE additions; NEW files only (resume_val.py, test_resume_val.py); never modify or break any existing file or test; keep the FULL existing suite green at every phase; every new function is pure (no I/O/network/global state) and covered by a new test asserting real values. PHASE 2 DELIBERATELY adds a slow test (a fixed time.sleep) to create the kill window — this is INTENTIONAL test infrastructure for resume validation, NOT a defect: do not block the plan over it (it is at most a style nit, never a P0/P1). Sandbox plan cap is $5; these phases are nearly free.

## Phases
- Phase 1: ✅ — Phase 1: Create a NEW module `resume_val.py` with a pure function `clamp(value, low, high)` that returns `value` bounded (commit 748640905d)
- Phase 2: ✅ — Phase 2: In the EXISTING `test_resume_val.py` (do NOT touch resume_val.py or Phase 1's test), add a NEW test function wh (commit eac7d73ef5)
- Phase 3: ✅ — Phase 3: In the EXISTING `resume_val.py` add a pure function `double(x)` returning `x * 2`, and in `test_resume_val.py`  (commit f3de29d4db)

## Final cross-model review (GPT-5.5 + Opus) — GATE
**Verdict:** ✅ mergeable — 0 blocking findings from the final gate.

## Cumulative proof (final test over base..HEAD)
```
$ python -m venv .venv && .venv/bin/pip install -q -r requirements.txt
[exit 0]

[notice] A new release of pip is available: 25.0.1 -> 26.1.2
[notice] To update, run: python -m pip install --upgrade pip

$ .venv/bin/pytest -q
[exit 0]
.....                                                                    [100%]
=============================== warnings summary ===============================
.venv/lib/python3.12/site-packages/_pytest/cacheprovider.py:469
  /app/runs/20260628-014729-sandbox-d179c3/final_verify/.venv/lib/python3.12/site-packages/_pytest/cacheprovider.py:469: PytestCacheWarning: could not create cache path /app/.pytest_cache/v/cache/nodeids: [Errno 13] Permission denied: '/app/pytest-cache-files-5fckpqi2'
    config.cache.set("cache/nodeids", sorted(self.cached_nodeids))

-- Docs: https://docs.pytest.org/en/stable/how-to/capture-warnings.html
5 passed, 1 warning in 120.02s (0:02:00)
```

## Spend (whole plan)
- Total: $0.1811 / plan cap $5.0000
- Calls: 11 (estimated: 0)
  - _estimated = the call's token usage couldn't be parsed, so it's charged its reserve: a conservative pre-call estimate, NOT a provider-enforced ceiling (reconciled calls carry real metered cost)._
- Tokens: in=98038 out=8132 cache_read=360936 cache_create=5293

_Generated by the Cloud Agent orchestrator (multi-phase plan)._